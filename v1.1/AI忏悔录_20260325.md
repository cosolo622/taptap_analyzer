# AI忏悔录 2026-03-25

## 今日最简上下文

- 项目：`taptap_analyzer/v1.1`
- 目标：把小红书舆情监控能力并入现有 TapTap 舆情平台
- 当前状态：小红书抓取已跑通，产品与技术规划文档已完成 v0

## 今日进展

- 完成项目文档梳理，确认后端多平台主干模型可复用
- 完成 MediaCrawler 小红书抓取链路验证并确认当日数据文件落地
- 输出《小红书舆情监控产品功能规划 v0》并补充项目上下文记录

## 今日错误与反思

- 错误：多次使用长参数命令时触发 PowerShell 输入异常，导致参数被截断并出现误判
- 修正：改用默认配置入口 `python main.py` 跑通，再做结果文件与日志双重校验
- 反思：遇到终端输入层问题时应先切换“最短可执行命令”，避免在参数层反复消耗

## 明日优先

- 完成小红书数据到 v1.1 PostgreSQL 的统一映射脚本
- 打通平台切换查询接口（TapTap/XHS/ALL）
- 输出预警规则首版并接入系统监控入口

## 今日补充进展（测试验收后）

- 已完成测试工程师三项验收：新文档核对通过、当日抓取文件核对通过、前端可访问性基础检查已执行
- 前端可启动但存在可访问性缺口：`h1` 缺失、部分输入与按钮无可访问名称
- 发现后端未连通时前端出现 `ERR_CONNECTION_REFUSED` 与 `500`，后续回归必须在后端联通态执行

## 今日关键卡点与忏悔

- 卡点：Playwright 在点击“社区洞察套件”时首次失败，原因是小红书子菜单未展开导致元素不可见
- 解决：先点击“小红书监控”展开子菜单，再点击“社区洞察套件”，QA全链路恢复通过并完成截图留证
- 忏悔：对复杂菜单结构不应直接点叶子节点，先验证层级展开状态能减少无效重试和误判

## 今日关键卡点与忏悔（Tab结构修正轮）

- 卡点：我上一版只做了单页模块堆叠，没有把“几个Tab、每个Tab下模块”结构抄到位，和目标产品心智不一致
- 解决：重构为两级Tab，一级“使用手册/产品功能”，二级含6个功能Tab，并全部绑定真实数据
- 二次卡点：在运行 dev 的终端直接执行 build 导致异常退出（5999）
- 修正：切换独立终端执行构建，`npm run build` 正常通过
- 忏悔：结构性需求必须先还原信息架构再编码，避免“内容对了但层级错了”的返工

## 今日关键卡点与忏悔（左侧菜单对齐轮）

- 卡点：我把结构放在页面内Tab，和“像TapTap一样放左侧菜单”的要求不一致
- 解决：将小红书模块改为左侧7菜单，并把模块内容改为按菜单路由显示
- 二次卡点：新增AI预留接口后首次请求404，原因是后端仍在旧进程未重启
- 修正：重启 uvicorn 后接口恢复，`/api/xhs/ai/config` 与 `/api/xhs/ai/analyze-batch` 可用
- 忏悔：后端新增路由后必须第一时间做“进程状态+接口烟测”双检查，避免把“代码已改”误判为“服务可用”

## 今日关键卡点与忏悔（定时开关与入库轮）

- 卡点：我先做了页面展示，但没有把“定时开关、历史抓取、入库机制”做成可执行闭环
- 解决：补上小红书独立抓取接口、4小时策略、手动增量/历史抓取、JSONL入库PostgreSQL全链路
- 二次卡点：对标文档细节在公开页可见内容有限，需通过目录锚点结构反推完整信息架构后再落地模块
- 修正：先抽取目录锚点完整结构，再按14项左侧菜单实现，并补充对标说明文档的逐项映射
- 忏悔：功能复刻必须遵循“结构先于内容、机制先于展示”，否则会出现看起来像但不可运营的问题
