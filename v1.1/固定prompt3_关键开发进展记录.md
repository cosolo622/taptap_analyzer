# 固定prompt3：关键开发进展记录

## 2026-03-25 关键进展 #001

- 现阶段做到：已完成 v1.1 项目文档梳理，明确现有主链路为 TapTap 采集-分析-入库-前端展示，且前端已预留“小红书监控”菜单位。
- 现阶段做到：已在 `MediaCrawler` 成功跑通小红书抓取，验证当日 JSONL 数据落地可用。
- 遇到并解决的坑：命令行参数执行过程中出现 PowerShell 输入异常，导致 `--type` 参数被破坏；改为使用默认配置执行 `python main.py` 规避并成功完成抓取。
- 关键上下文：小红书接入采用“采集层分开、分析层合并、数据库统一”的路线，先保证稳定性和可运营性。
- 后续规划：先做统一入库映射与平台切换展示，再做预警与竞品开关，最后完善 KOL 与版本口碑分析。

## 2026-03-25 关键进展 #002

- 现阶段做到：已完成本轮测试验收三项核对（新文档存在性与完整性、小红书当日抓取文件存在性、前端基础可访问性 QA）。
- 现阶段做到：前端 `npm run dev -- --host 127.0.0.1 --port 4173` 已成功启动，并可访问 `http://127.0.0.1:4173/`。
- 遇到并解决的坑：既有终端触发 PSReadLine 渲染异常（`ArgumentOutOfRangeException`）导致命令输入中断；通过启用新终端重启前端服务后恢复正常。
- 关键上下文：可访问性快速检查发现 `h1` 缺失、部分控件缺少可访问名称；同时存在后端未连通导致的 `ERR_CONNECTION_REFUSED` 控制台错误。
- 后续规划：先补齐语义标题与控件 `aria-label`/可读标签，再在后端联通态下执行一轮完整 A11y 回归（键盘导航+表单可读性+错误提示）。

## 2026-03-25 关键进展 #003

- 现阶段做到：已完成“小红书功能验收”4项核对（左侧菜单存在性、6个功能菜单完整性、帖子明细跳转链接、AI分析接口预留可调用）。
- 现阶段做到：前端在 `http://127.0.0.1:4174/` 可见“小红书监控”及 7 个子菜单（含“使用手册”+6个功能菜单），并在“社区内容分析”页检出有效外链。
- 遇到并解决的坑：原定端口 `4173` 被占用导致 Vite 自动切换 `4174`；按新端口完成 Playwright 自动化验收，避免误判“页面不可达”。
- 关键上下文：AI接口当前为预留态，`POST /api/xhs/ai/analyze-batch` 返回 `accepted=true、configured=false`，符合“可调用但待配置模型”设计。
- 后续规划：下一步可补充 AI 接口联调用例（鉴权、超时、重试、失败提示）并纳入回归脚本。
